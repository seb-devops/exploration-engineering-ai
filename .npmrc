# pnpm configuration
# Enable strict peer dependencies (recommended for monorepos)
strict-peer-dependencies=false

# Use the same node version across the project
# Uncomment and set your Node.js version if needed
# use-node-version=20.9.0

# Store dependencies in a global store (pnpm's default behavior)
# This saves disk space by deduplicating packages across projects
store-dir=.pnpm-store

# Auto-install peers (useful for monorepos)
auto-install-peers=true

# Shamefully hoist dependencies (may be needed for some tools)
# Uncomment if you encounter issues with nested dependencies
# shamefully-hoist=false

